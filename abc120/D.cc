#include <bits/stdc++.h>
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

int main(){

	int n, m;
	long long int c=0;

	cin>> n >> m;
	c=n*(n-1)/2;

	a.resize(m);
	b.resize(m);
	con.resize(n);

	for(int i=0;i<m;i++) cin>> a[i] >> b[i];
	fill(con.begin(), con.end(), 0);

	for(int i=n-1;0<=i;i--){
		con[i]
		a[i] b[i]

	cout<< n << endl;
}
